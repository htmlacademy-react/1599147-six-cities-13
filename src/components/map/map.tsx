// type MapProps = {

// }

export default function Map(): JSX.Element {
  return (
    <section className="cities__map map"
      style={{
        height: `${794}px`
      }}
    >
    </section>
  );
}
